/// Defines domain-level requirements for interaction availability.
///
/// This keeps use cases and orchestrators aligned on shared business
/// constraints such as key dependencies without duplicating literals.
const Map<String, int> kOpenObjectRequiredKeys = <String, int>{'GRATE': 1};
