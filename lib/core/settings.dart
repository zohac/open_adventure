class Settings {
  final bool oldStyle;
  final bool prompt;
  final int debug;
  // Autres paramètres si nécessaire

  Settings({
    required this.oldStyle,
    required this.prompt,
    required this.debug,
  });
}
