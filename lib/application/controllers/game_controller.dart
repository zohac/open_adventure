/// GameController — Application layer orchestrator (S1 scaffold).
///
/// Holds no logic yet in S1; concrete implementation will arrive in S2
/// (navigation by buttons, autosave, state exposure).
class GameController {
  /// Placeholder init method for S1 scaffolding.
  void init() {
    // No-op in S1.
  }
}

